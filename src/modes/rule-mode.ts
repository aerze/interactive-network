import { BaseMode } from "./base-mode";

/**
 * Rules Mode
 * - add rule to device
 * - delete rule from device
 * - select which rule to add, with options
 */
export class RuleMode extends BaseMode {}
